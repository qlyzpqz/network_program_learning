#include <stdio.h>

int read_n(char *buffer, int n);
int write_n(char *buffer, int n);

int receive(char *ip, int port);
int transmit(int message_cnt, int message_length);

int main (int argc, char **argv)
{
    return 0;
}
